116
# Play(내부·닫힌·프로덕션 아무 트랙)에 이미 올라간 마지막 versionCode.
# 다음 AAB는 이 숫자보다 커야 한다. Codemagic 안내 전에 에이전트가 읽고 pubspec과 비교한다.
# 이 채팅에서 88~99 Codemagic/Play(비공개 테스트) 업로드를 안내하면 그 번호는 사용한 것으로 친다.
# 100·101 은 이미 사용. 102 는 iOS 운영 심사 빌드. 103 은 Play에 이미 있음.
# 104·105 는 이 채팅에서 Codemagic/Play 안내함. 106 은 알라딘 명단·랭킹 +5. 107 은 시상 횟수 모임 분리.
# 108 은 알라딘 회원 2명이 앱에 안 보이던 명단 반영. 109 는 기능 추가 없이 빌드 안내.
# 110 은 D-1을 미응답만으로 좁힘. 111 은 D-1을 참석 회원으로 되돌림.
# 112 는 남의 모임이 내 모임으로 보이던 것 정리. 113 은 전화번호로 모임 자동 연결.
# 112·113 은 빌드 안 하고 넘어감. 114 가 실제 업로드 대상 (동기화 복구 + 재무·시상 UI).
# 115 는 같은 사람이 명단에 두 줄로 생기던 것 자동 병합. 이 채팅에서 +115 커밋 후 UI가 더 들어가 116으로 올린다.
# 116 은 명단 병합·사진 복구·회비납부 아바타·모임정보수정 시·도 칩까지 일괄.
# 117 은 일정변경 참석 유지·D-day 확대·일정/재무 카드 테두리. 이 채팅에서 Codemagic/Play 안내.
